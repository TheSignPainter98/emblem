export hello = (name='current') ->
	print "#{name} world!"

hello!

print em
-- em.cmds.asdf = -> 'fdsa'

$tests ->
	import 'busted' as :assert, :describe, :it
	describe 'emblem', ->
		it 'is amazing', ->
			assert.truthy false
