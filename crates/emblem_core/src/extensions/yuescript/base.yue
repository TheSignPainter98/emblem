export hello = (name='current') ->
	print "#{name} world!"

hello!

print em
-- em.cmds.asdf = -> 'fdsa'

$spec ->
	import 'busted' as :assert, :describe, :it

	describe 'emblem', ->
		it 'is amazing', ->
			assert.is.true true
